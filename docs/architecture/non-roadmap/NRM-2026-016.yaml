schema: "kumwe-non-roadmap-record/v1"
id: "NRM-2026-016"
nrm_ref: "NRM-2026-016"
roadmap_impact: "None"
rationale: "Provision the native dependency and compose the public Computation services needed by extracted package consumers."
repositories:
  - "kumwe/app"
  - "kumwe/computation"
  - "kumwe/kumwe-engine"
  - "kumwe/engine"
behavior_before: "App has no provisioned native runtime or shared native canonical, compiler and executor services."
behavior_after: "App admits the independently recorded native build and shares the public package services through its composition root."
public_api_impact: "Host configuration adds KUMWE_NATIVE_EXPECTED_TUPLE; extension providers receive the existing CanonicalEncoder contract."
dependency_impact: "Adds exact kumwe/computation 0.3.3 and ext-kumwe_engine 1.0.3 requirements."
compatibility: "Existing App consumers retain their behavior; this prerequisite establishes package service composition before their adoption."
data_migration: "None"
security_impact: "Provisioning checks published source digests; boot refuses an absent or incompatible independently recorded native tuple."
performance:
  claim: "None"
  evidence: []
operations_deployment: "Production, development and CI builds provision the same pinned release; the deployment tuple is retained outside application code."
recovery_rollback: "Revert PR140 and rebuild the previous images; no persisted data changes."
artifacts_evidence:
  - "docs/architecture/migrations/KUMWE-MIG-2026-008.yaml"
  - "docs/architecture/migrations/change-sets/KUMWE-CS-2026-008.yaml"
  - "docs/architecture/migrations/trains/KUMWE-TRAIN-2026-008.yaml"
  - "docs/architecture/migrations/evidence/KUMWE-MIG-2026-008/RELEASE-ATTESTATION.yaml"
  - "resources/native-runtime/source.json"
  - "tools/install-native-engine.sh"
  - "tests/Unit/Kernel/NativeComputationFactoryTest.php"
known_gaps:
  - "Legacy calculation consumers migrate with their owning packages after this prerequisite lands."
  - "The final App commit must pass all required hosted checks before merge."
owner: "Codex under maintainer delegation"
status: "in-progress"
